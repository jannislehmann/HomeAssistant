# lovelace_gen
type: horizontal-stack
cards:
- template: chips_return
  {% if navigation_path %}
  variables:
    navigation_path: {{ navigation_path }}
  {% endif %}
  type: 'custom:button-card'
- template: title
  name: {{ title }}
  type: 'custom:button-card'