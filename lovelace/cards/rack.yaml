type: vertical-stack
cards:
  - template: title
    name: Rack
    type: 'custom:button-card'

  - type: vertical-stack
    cards:
      - type: horizontal-stack
        cards:
        - type: custom:button-card
          template: graph
          variables:
            entity: sensor.temperature_rack
            color: "var(--google-red)"
            name: Temperatur
        - type: custom:button-card
          template: graph
          variables:
            entity: sensor.humidity_rack
            color: "var(--google-blue)"
            name: Luftfeuchtigkeit
