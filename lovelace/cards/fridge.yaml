type: vertical-stack
cards:
  - template: title
    name: Kühlschrank
    type: 'custom:button-card'

  - type: vertical-stack
    cards:
      - type: horizontal-stack
        cards:
        - type: custom:button-card
          template: graph
          variables:
            entity: sensor.temperature_fridge
            color: "var(--google-red)"
            name: Temperatur
        - type: custom:button-card
          template: graph
          variables:
            entity: sensor.humidity_fridge
            color: "var(--google-blue)"
            name: Luftfeuchtigkeit
      - type: custom:button-card
        template: graph
        variables:
          entity: sensor.fridge_pressure
          color: "var(--google-gray)"
          name: Luftdruck
